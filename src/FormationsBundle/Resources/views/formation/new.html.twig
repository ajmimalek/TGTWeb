{% extends 'profile.html.twig' %}
{% block name  %}
Ajout Formation
{% endblock%}
{% block content %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_errors(form) }}
    {{ form_end(form) }}


    <ul>
        <li>
            <a href="{{ path('formation_index') }}" class="btn btn-primary">Back to the list</a>
        </li>
    </ul>
{% endblock %}

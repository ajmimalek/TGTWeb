{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorieformation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieFormation.id }}</td>
            </tr>
            <tr>
                <th>Libele</th>
                <td>{{ categorieFormation.libele }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorieformation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorieformation_edit', { 'id': categorieFormation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
